# Build artifacts
knowledge
/target/

# Cargo.lock is typically committed for binaries, but ignore if you prefer
# Cargo.lock

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# FFmpeg sidecar downloads
ffmpeg/
ffprobe/

# Config files (user-specific)
# ~/.config/gifer/ is where settings are stored, not in the project

# Security - prevent accidental commits
*.pem
*.key
*.p12
*.pfx
.env*
*secret*
*credential*
*.asc
gifer-0.2.0-darwin-arm64.tar.gz.asc
